- `url`: the ankiquest server, e.g. `https://anki.example.com`
- `user`: your player name
- `token`: your upload token

Only review log rows (card id, timestamp, previous interval, time taken, review type) are sent, never card content.
